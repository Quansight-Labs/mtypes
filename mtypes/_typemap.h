/*
 * Typemap stores all associated information
 * and creates a generic hashmap dict that 
 * does logcial type-checking. This needs to
 * be done manually for each binding. All this does is
 * generate a dictionary that can be populated by 
 * each binding.
 * 
 * k-> key, which
 */

#ifndef TYPEMAP_H
#define TYPEMAP_H
#include "klib/khash.h"

int init_dict(...)


#endif